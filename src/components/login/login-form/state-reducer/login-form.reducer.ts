import { ReducerDefaultInterface } from "../../../shared/types/use-reducer-default.type";
import { LoginFormReducerActionsEnum } from "./login-form.actions";

export function loginFormStateReducer(state: any, action: ReducerDefaultInterface<LoginFormReducerActionsEnum>) {
  switch(action.type) {
    default: {
      break;
    }
  }
}
